# Example: Copy this to your repo as .github/workflows/dependabot-auto-merge.yml
name: Dependabot Auto-merge

on:
  pull_request:

permissions:
  contents: read

jobs:
  auto-merge:
    uses: cuioss/cuioss-organization/.github/workflows/reusable-dependabot-auto-merge.yml@f3b0586c485fa1f4ea8e004bd56f27f0a7d280df # v0.23.0
    permissions:
      contents: write
      pull-requests: write
